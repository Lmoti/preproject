import styled from "styled-components";

const StyledHeader = styled.h1`
  color: #555;
  font-size: 8rem;
  width: fit-content;
`;
export default function Header1(childern) {
  return <StyledHeader />;
}
